'''
get image h & w
get pixel-length ratio

for each pixel in image:
    instantiate floor node at p[x]*tileWidth, p[y]*tileHeight, probably a staticbody3d with a plane mesh

#improved algorithm below
first, find matching shapes:
    for each of these shapes, make a rectangle mesh for it
'''